// let heading = (document.getElementById("title").style.backgroundColor = "red");
let heading = (document.getElementById("title").innerText = "vikash sharma");

// querySelectorAll give NodeList node array its similer to array but not array
